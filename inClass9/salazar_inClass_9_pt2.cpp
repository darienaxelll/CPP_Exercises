#include <iostream>
#include <string>

using namespace std;

int main () {
    string foodItems[] = {"eggplant", "squash", "apples", "hamburger", "pizza", "shampoo", "soap", "laundry", "detergent", "bacon"};
    int arrSize = sizeof(foodItems)/sizeof(foodItems[0]);

    // sort(foodItems, (arrSize - 1));

    return 0;
}